import { AdminProductSearchPipe } from './admin-product-search.pipe';

describe('AdminProductSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminProductSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
