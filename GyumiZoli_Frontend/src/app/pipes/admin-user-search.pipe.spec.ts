import { AdminUserSearchPipe } from './admin-user-search.pipe';

describe('AdminUserSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUserSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
